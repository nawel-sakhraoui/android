import { ExistEmailValidatorDirective } from './exist-email-validator.directive';

describe('ExistEmailValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new ExistEmailValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
